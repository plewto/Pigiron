package main

import (
	"testing"
	// "github.com/plewto/pigiron/op"
)

func TestBasics(t *testing.T) {
	// alpha, _ := op.MakeOperator("Null", "Alpha")
	// cmode := alpha.ChannelMode()
	// if cmode != op.NoChannel {
	// 	t.Fatalf("Unexpected channel mode: %v", cmode)
	// }
}
